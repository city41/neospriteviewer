import React from "react";

interface HeaderProps {
    className?: string;
}

const Header: React.StatelessComponent<HeaderProps> = ({ className }) => {
    return <header>header</header>;
};

export { Header };
